# Backend API base URL
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Temporary placeholder until Phase 8 auth is wired up.
# Set this to a real user UUID from your seeded database.
# After Phase 8 this is replaced by the authenticated user's ID automatically.
NEXT_PUBLIC_DEV_CREATOR_ID=1
